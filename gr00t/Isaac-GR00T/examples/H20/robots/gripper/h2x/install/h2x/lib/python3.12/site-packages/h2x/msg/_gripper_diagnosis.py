# generated from rosidl_generator_py/resource/_idl.py.em
# with input from h2x:msg/GripperDiagnosis.idl
# generated code does not contain a copyright notice

# This is being done at the module level and not on the instance level to avoid looking
# for the same variable multiple times on each instance. This variable is not supposed to
# change during runtime so it makes sense to only look for it once.
from os import getenv

ros_python_check_fields = getenv('ROS_PYTHON_CHECK_FIELDS', default='')


# Import statements for member types

import builtins  # noqa: E402, I100

# Member 'error_code'
# Member 'warning_code'
import numpy  # noqa: E402, I100

import rosidl_parser.definition  # noqa: E402, I100


class Metaclass_GripperDiagnosis(type):
    """Metaclass of message 'GripperDiagnosis'."""

    _CREATE_ROS_MESSAGE = None
    _CONVERT_FROM_PY = None
    _CONVERT_TO_PY = None
    _DESTROY_ROS_MESSAGE = None
    _TYPE_SUPPORT = None

    __constants = {
    }

    @classmethod
    def __import_type_support__(cls):
        try:
            from rosidl_generator_py import import_type_support
            module = import_type_support('h2x')
        except ImportError:
            import logging
            import traceback
            logger = logging.getLogger(
                'h2x.msg.GripperDiagnosis')
            logger.debug(
                'Failed to import needed modules for type support:\n' +
                traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg__gripper_diagnosis
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg__gripper_diagnosis
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg__gripper_diagnosis
            cls._TYPE_SUPPORT = module.type_support_msg__msg__gripper_diagnosis
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg__gripper_diagnosis

            from h2x.msg import GripperDiagnosticData
            if GripperDiagnosticData.__class__._TYPE_SUPPORT is None:
                GripperDiagnosticData.__class__.__import_type_support__()

    @classmethod
    def __prepare__(cls, name, bases, **kwargs):
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
        }


class GripperDiagnosis(metaclass=Metaclass_GripperDiagnosis):
    """Message class 'GripperDiagnosis'."""

    __slots__ = [
        '_ds_data',
        '_error_code',
        '_warning_code',
        '_crc',
        '_check_fields',
    ]

    _fields_and_field_types = {
        'ds_data': 'h2x/GripperDiagnosticData[2]',
        'error_code': 'int32[2]',
        'warning_code': 'int32[2]',
        'crc': 'uint32',
    }

    # This attribute is used to store an rosidl_parser.definition variable
    # related to the data type of each of the components the message.
    SLOT_TYPES = (
        rosidl_parser.definition.Array(rosidl_parser.definition.NamespacedType(['h2x', 'msg'], 'GripperDiagnosticData'), 2),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.BasicType('int32'), 2),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.BasicType('int32'), 2),  # noqa: E501
        rosidl_parser.definition.BasicType('uint32'),  # noqa: E501
    )

    def __init__(self, **kwargs):
        if 'check_fields' in kwargs:
            self._check_fields = kwargs['check_fields']
        else:
            self._check_fields = ros_python_check_fields == '1'
        if self._check_fields:
            assert all('_' + key in self.__slots__ for key in kwargs.keys()), \
                'Invalid arguments passed to constructor: %s' % \
                ', '.join(sorted(k for k in kwargs.keys() if '_' + k not in self.__slots__))
        from h2x.msg import GripperDiagnosticData
        self.ds_data = kwargs.get(
            'ds_data',
            [GripperDiagnosticData() for x in range(2)]
        )
        if 'error_code' not in kwargs:
            self.error_code = numpy.zeros(2, dtype=numpy.int32)
        else:
            self.error_code = kwargs.get('error_code')
        if 'warning_code' not in kwargs:
            self.warning_code = numpy.zeros(2, dtype=numpy.int32)
        else:
            self.warning_code = kwargs.get('warning_code')
        self.crc = kwargs.get('crc', int())

    def __repr__(self):
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args = []
        for s, t in zip(self.get_fields_and_field_types().keys(), self.SLOT_TYPES):
            field = getattr(self, s)
            fieldstr = repr(field)
            # We use Python array type for fields that can be directly stored
            # in them, and "normal" sequences for everything else.  If it is
            # a type that we store in an array, strip off the 'array' portion.
            if (
                isinstance(t, rosidl_parser.definition.AbstractSequence) and
                isinstance(t.value_type, rosidl_parser.definition.BasicType) and
                t.value_type.typename in ['float', 'double', 'int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'int64', 'uint64']
            ):
                if len(field) == 0:
                    fieldstr = '[]'
                else:
                    if self._check_fields:
                        assert fieldstr.startswith('array(')
                    prefix = "array('X', "
                    suffix = ')'
                    fieldstr = fieldstr[len(prefix):-len(suffix)]
            args.append(s + '=' + fieldstr)
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        if self.ds_data != other.ds_data:
            return False
        if any(self.error_code != other.error_code):
            return False
        if any(self.warning_code != other.warning_code):
            return False
        if self.crc != other.crc:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls):
        from copy import copy
        return copy(cls._fields_and_field_types)

    @builtins.property
    def ds_data(self):
        """Message field 'ds_data'."""
        return self._ds_data

    @ds_data.setter
    def ds_data(self, value):
        if self._check_fields:
            from h2x.msg import GripperDiagnosticData
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) == 2 and
                 all(isinstance(v, GripperDiagnosticData) for v in value) and
                 True), \
                "The 'ds_data' field must be a set or sequence with length 2 and each value of type 'GripperDiagnosticData'"
        self._ds_data = value

    @builtins.property
    def error_code(self):
        """Message field 'error_code'."""
        return self._error_code

    @error_code.setter
    def error_code(self, value):
        if self._check_fields:
            if isinstance(value, numpy.ndarray):
                assert value.dtype == numpy.int32, \
                    "The 'error_code' numpy.ndarray() must have the dtype of 'numpy.int32'"
                assert value.size == 2, \
                    "The 'error_code' numpy.ndarray() must have a size of 2"
                self._error_code = value
                return
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) == 2 and
                 all(isinstance(v, int) for v in value) and
                 all(val >= -2147483648 and val < 2147483648 for val in value)), \
                "The 'error_code' field must be a set or sequence with length 2 and each value of type 'int' and each integer in [-2147483648, 2147483647]"
        self._error_code = numpy.array(value, dtype=numpy.int32)

    @builtins.property
    def warning_code(self):
        """Message field 'warning_code'."""
        return self._warning_code

    @warning_code.setter
    def warning_code(self, value):
        if self._check_fields:
            if isinstance(value, numpy.ndarray):
                assert value.dtype == numpy.int32, \
                    "The 'warning_code' numpy.ndarray() must have the dtype of 'numpy.int32'"
                assert value.size == 2, \
                    "The 'warning_code' numpy.ndarray() must have a size of 2"
                self._warning_code = value
                return
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) == 2 and
                 all(isinstance(v, int) for v in value) and
                 all(val >= -2147483648 and val < 2147483648 for val in value)), \
                "The 'warning_code' field must be a set or sequence with length 2 and each value of type 'int' and each integer in [-2147483648, 2147483647]"
        self._warning_code = numpy.array(value, dtype=numpy.int32)

    @builtins.property
    def crc(self):
        """Message field 'crc'."""
        return self._crc

    @crc.setter
    def crc(self, value):
        if self._check_fields:
            assert \
                isinstance(value, int), \
                "The 'crc' field must be of type 'int'"
            assert value >= 0 and value < 4294967296, \
                "The 'crc' field must be an unsigned integer in [0, 4294967295]"
        self._crc = value
